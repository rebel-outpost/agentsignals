$('.modal-footer').remove();
$('.modal-title').html("New Task");
$('.modal-body p').html("<%= j render partial: 'events/task_form', locals: {ajax: true} %>");
$('.datetime-picker').datetimepicker({
  format: 'M d, Y h:i a', 
  step: 30
});